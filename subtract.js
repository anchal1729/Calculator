//subtraction function

function subtractNumbers(num1, num2) {
    return num1 - num2;
}

// Example usage:
var number1 = 25;
var number2 = 15;
var subtractResult = subtractNumbers(number1, number2);
console.log(`The subtraction of ${number1} and ${number2} is: ${subtractResult}`);
